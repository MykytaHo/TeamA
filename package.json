{
  "name": "TeamA",
  "description": "Local Services Marketplace Application",
  "scripts": {
    "start": "cd my-app && npm run dev",
    "dev": "cd my-app && npm run dev",
    "build": "cd my-app && npm run build",
    "preview": "cd my-app && npm run preview",
    "install:app": "cd my-app && npm install"
  },
  "dependencies": {
    "firebase": "^12.9.0"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
